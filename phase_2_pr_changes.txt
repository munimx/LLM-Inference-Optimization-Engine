In scripts/run_benchmarks.py:

> +        "--all", action="store_true", help="Run benchmark over all discovered models"
+    )
+    parser.add_argument("--family", type=str, help="Benchmark a specific model family")
+    parser.add_argument("--model", type=str, help="Benchmark a specific model")
+    parser.add_argument(
The CLI defines --all, --family, and --model, but run() never uses them to filter which models are benchmarked. As a result, users passing --family/--model will still benchmark whatever BenchmarkSuite discovers. Implement argument handling (e.g., filter the discovered models list, or call collector.collect_model_family(...), or benchmark a single model) or remove these flags to avoid misleading behavior.


In scripts/run_benchmarks.py:

> +    perf_cfg = benchmark_cfg.get("performance", {})
+    scenario = perf_cfg.get("scenarios", [{}])[0]
+    if not isinstance(scenario, dict):
+        scenario = {}
scenarios is a list in the YAML config, but the script always selects perf_cfg['scenarios'][0], ignoring the rest (and also ignoring any CLI selection). Either iterate scenarios and run them all, or add an argument to select by scenario name so the config behaves as documented.


In src/llm_inference_engine/quantization/benchmarks.py:

> +        memory_profiler = MemoryProfiler() if self._config.measure_memory else None
+        throughputs: list[float] = []
MemoryProfiler() here measures the RSS of the current Python process (the benchmark runner), but generation happens in the separate Ollama service process. This makes memory_peak_mb / memory_baseline_mb misleading as “model memory” metrics. Consider measuring the Ollama process by PID, using model-size-based estimates, or renaming these fields to clarify they’re client-process memory.


In src/llm_inference_engine/quantization/mapper.py:

> +        speed = min((model.tokens_per_second or 0.0) / 100.0, 1.0)
+        quality = model.quality_score or 0.0
+        memory_efficiency = 1.0 / max(model.memory_estimate_gb, 0.001)
+        if preference.priority == PreferencePriority.SPEED:
+            return (0.65 * speed) + (0.2 * quality) + (0.15 * memory_efficiency)
+        if preference.priority == PreferencePriority.BALANCED:
+            return (0.4 * speed) + (0.4 * quality) + (0.2 * memory_efficiency)
+        if preference.priority == PreferencePriority.QUALITY:
+            return (0.05 * speed) + (0.9 * quality) + (0.05 * memory_efficiency)
+        return (0.2 * speed) + (0.2 * quality) + (0.6 * memory_efficiency)
memory_efficiency = 1 / memory_estimate_gb is unbounded and not normalized to the same scale as speed and quality (which are capped to ~[0,1]). For models with <1GB estimates this can dominate the weighted score and effectively override the chosen PreferencePriority. Consider normalizing memory efficiency (e.g., min/max across candidates, log-scaling, or converting to a bounded score) before weighting.


In src/llm_inference_engine/quantization/metrics.py:

> +    async def run_quality_comparison(self, models: list[str]) -> dict[str, QualityScore]:
+        """Run quality benchmark for each model and return aggregated scores."""
+        results: dict[str, QualityScore] = {}
+        for model in models:
+            bleu_scores: list[float] = []
+            rouge1_scores: list[float] = []
+            semantic_scores: list[float] = []
+            for test_case in self._test_dataset:
+                response = await self._client.generate(
+                    model=model,
+                    prompt=test_case.prompt,
+                    max_tokens=test_case.max_tokens,
+                )
+                generated = str(response.get("response", ""))
+                bleu = self._calculator.calculate_bleu(generated, test_case.reference_outputs)
+                rouge = self._calculator.calculate_rouge(generated, test_case.reference_outputs)
+                semantic = mean(
+                    self._calculator.calculate_semantic_similarity(generated, reference)
+                    for reference in test_case.reference_outputs
+                )
+                bleu_scores.append(bleu)
+                rouge1_scores.append(rouge["rouge1"])
+                semantic_scores.append(semantic)
+
+            overall = (
+                (mean(bleu_scores) * 0.4)
+                + (mean(rouge1_scores) * 0.3)
+                + (mean(semantic_scores) * 0.3)
+            )
If self._test_dataset is empty, bleu_scores / rouge1_scores / semantic_scores stay empty and the subsequent mean(...) calls will raise StatisticsError. Add an explicit check (e.g., validate non-empty dataset in __init__, or return 0-scores when no test cases are provided) so run_quality_comparison() fails gracefully or deterministically.


In PHASE_2_COMPLETE.md:

> +- Machine-readable benchmark output: `tests/benchmarks/benchmark_results/benchmark_results.json`
+- Human-readable benchmark report: `docs/benchmark_results.md`
This section points to the machine-readable benchmark output under tests/benchmarks/benchmark_results/benchmark_results.json, but this PR also adds docs/benchmark_results.json (and the CLI default --output is tests/benchmarks/benchmark_results). Consider clarifying which path is the generated runtime output vs which file is the committed “latest” sample, or update the reference to match the intended source of truth.


In scripts/run_benchmarks.py:

> +                    "name": row["model_name"],
+                    "family": row["model_name"].split(":")[0],
+                    "quantization": row.get("quantization", "unknown"),
+                    "size_bytes": 0,
+                    "memory_estimate_gb": float(row.get("memory_peak_mb", 0.0)) / 1024,
+                    "parameters": None,
+                    "context_length": 4096,
+                    "quality_tier": "acceptable",
+                    "format": "gguf",
+                    "tokens_per_second": float(row.get("tokens_per_second", 0.0)),
+                    "time_to_first_token_ms": float(row.get("time_to_first_token_ms", 0.0)),
+                    "memory_peak_mb": float(row.get("memory_peak_mb", 0.0)),
+                    "quality_score": float(
+                        quality_scores.get(row["model_name"], {}).get("overall_score", 0.0)
+                    ),
+                }
memory_estimate_gb is derived from memory_peak_mb / 1024, which is both (1) based on the benchmark runner process RSS (not Ollama’s model memory), and (2) produces unrealistically small GB values that will heavily skew QuantizationMapper scoring toward “memory efficient” models for all priorities. Prefer using ModelInfo.memory_estimate_gb/size_bytes-based estimates from the model manager/collector, and keep benchmark-process RSS as a separate metric if needed.


In src/llm_inference_engine/quantization/benchmarks.py:

> +        """Estimate TTFT and total latency from sampled latencies."""
+        if not latencies:
+            return 0.0, 0.0
+        avg_latency = mean(latencies)
+        return avg_latency * 0.2, avg_latency
time_to_first_token_ms is currently derived as a fixed 20% of average latency. This is not TTFT and will mislead downstream reporting/comparisons. If Ollama exposes a first-token timing (or prompt eval duration) use that; otherwise consider recording TTFT separately by streaming tokens or label this as an estimate rather than TTFT.


In src/llm_inference_engine/quantization/benchmarks.py:

> +    def _extract_quantization(self, model_name: str) -> str:
+        """Extract quantization suffix from model name."""
+        if "-q" in model_name:
+            return model_name.split("-")[-1]
+        if ":q" in model_name:
+            return model_name.split(":")[-1]
+        return "unknown"
_extract_quantization() only detects quantization when the model name contains -q or :q, so common variants like *-fp16 (and any names without the -q pattern) will be reported as unknown. Consider reusing the collector’s regex-based parsing (QuantizationLevel) or extending this extraction to handle fp16/f16 and other naming conventions.


In src/llm_inference_engine/quantization/benchmarks.py:

> +            measure_quality=True,
+            timeout_seconds=config.timeout_seconds,
+        )
+        return await self.run_performance_suite(quality_config)
run_quality_suite() sets measure_quality=True but returns run_performance_suite(...) without actually computing/returning any quality metrics, and the returned payload still uses type: performance. Either remove measure_quality from BenchmarkConfig/this path, or have the suite compute and attach quality_scores (and use a distinct type) so callers can rely on the API.